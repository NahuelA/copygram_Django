{
    "builds": [{
        "src": "config/wsgi.py",
        "use": "@vercel/python",
        "dependencies": "requirements/production.txt",
        "config": { "maxLambdaSize": "15mb", "runtime": "python3.9" }
    }],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "config/wsgi.py"
        }
    ],
    "framework": "django",
    "packageManager": "pip@3.0.0",
    "installCommand": "pip install -v requirements/production.txt"
}